{
  "dependencies": {
    "faz-um-pix": "^2.0.2",
    "gpix": "^1.1.7",
    "puppeteer": "^19.4.1",
    "telegraf": "^4.8.5",
    "telegram-keyboard": "^2.3.1"
  },
  "scripts": {
     "start": "node index",
    "build": "npm install && npm run build"
  }
}
